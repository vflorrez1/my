import './App.css';
import { FC } from 'react';

const App: FC = () => (
  <div className="App" style={{ display: 'flex' }}>
    <div style={{ display: 'flex', flex: 1 }}>My</div>
  </div>
);
export default App;
